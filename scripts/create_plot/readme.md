# Plot (Docker feedback)

To use as a jupyter notebook, convert with `jupytext`

To preview in black and white:

```bash
convert dockerfeedback_new.png -type Grayscale dockerfeedback_new_gray.png
```
